# 计划 E1 / 目标：可靠性迭代

执行前固定门槛：必需AC全部有证据，无阻断问题；总分>=90，各适用维度>=80%。自评不等于独立审计。每维度项按0/0.5/1计分，权重30/20/15/15/10/10，N/A按剩余权重归一化；见各审查记录。

## 固定评分检查项（以下均必需）

- 功能30：本步列出的每一AC各1项，结果完整才得1。
- 一致性20：本步P/F/A映射落实；无扩大范围，各1项。
- 质量15：调用链/实现审阅；文档和错误处理一致，各1项。
- 证据15：当前工作树对应检查；失败尝试保留，各1项。
- UI10：仅T003适用，UI-001与UI-002各1项；T001文档/工作区，T002模型边界，T004打包交付不修改产品UI，执行前定为N/A，不赠分。
- 可靠性10：本步数据/既有修改保留；失败不假报成功，各1项。

## T001 评估、DevEco工作区与基线

来源E1，P-001/003，F-001/004，A-001，AC-001。无依赖。输入当前源代码、AGENTS和旧报告。交付assessment/blueprint/ui-spec/plan及新基线证据，引用旧文档避免双重定义；不更改业务。检查DevEco标题/日志、模型/页面/CI/依赖和测试构建。回退仅移除本轮新增文档，不改变旧实现。状态passed，轮次2，reviews/T001.md。

## T002 档案边界修复

依赖T001 passed。来源E1，P-001/002，F-002，A-001/002，AC-002。变更Observation.ts和规则测试，不改UI/存储接口/权限。先写失败测试，再做UTF8/保留ID边界；交付兼容合法Schema1的模型。检查边界测试、全部故障测试、ArkTS编译；证据T002-red/T002-tests/build。回退仅本步模型与新增测试，不回退上轮代码。状态passed，轮次3，reviews/T002.md。

## T003 验收脚本与真实模拟器回归

依赖T002 passed。来源E1，P-001/003，F-001/003，A-001/003，AC-003/004，UI-001/002。修改脚本，输出独立目录与失败证据，移除不安全resume，检查命令结果；交付脚本单元测试及一次完整模拟器回归和截图。目标UI维度适用，截图必须实际检查。回退可恢复旧脚本但不能复用旧成功证据；不删除未知设备数据。状态passed，轮次2，reviews/T003.md。

## T004 整体交付验收

干净构建依赖T002 passed，可与模拟器操作独立执行；整体验收结论及passed仍依赖T003 passed。来源E1，P-001/002/003，F-004，A-001，AC-005。在临时新工程验证README的构建和测试，不修改其他项目；复核追溯表与T003截图证据，生成release-review/state。交付工作树摘要、HAP完整性及启动说明。回退仅此次文档修改。状态blocked（全仓guard原始26号缺AGENTS），轮次1，reviews/T004.md。

后续未排期：真实设备签名、深色/大字号/读屏/平板、极端磁盘故障、商店资料与用户访谈，不自动添加相机/识别/社区。
