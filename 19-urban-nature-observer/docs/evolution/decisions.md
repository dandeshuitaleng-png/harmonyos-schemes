# 决策记录

- D-001 / 2026-09-23：占位URL按会话目标解释为19号，复用已有独立克隆而非重复克隆。若用户提供另一个真实URL则另建工作区，不更改当前origin。
- D-002：用户明确IDE是DevEco；已打开并验证独立项目窗口，Cursor工作区降为历史辅助，不作为DevEco验收依据。
- D-003：E1以边界可靠性为本轮版本，不把无签名与全设备验收包装成发布完成。保持正常Schema1，不引入数据库/账号。异常all ID拒绝加载，避免与已有清空操作语义混淆（P-001/F-002/A-002/AC-002）。

- D-004：复核SDK26类型声明发现MAX_VALUE_LENGTH已注明16MB，纠正旧测试与初稿“8192为SDK限制”的假设。采用应用自定8KiB档案预算以约束10条固定字段记录，不声称平台只支持8KiB。正常UI生成的数据远低于预算；超预算异常档案保留原文件并拒绝覆盖。AC-002的值不变，依据改为明确应用预算。

- D-005：T002测试发现十条控制字符ID仅6345字节，原溢出fixture不足。保留失败日志，改用含额外转义字段的异常档案产生实际>8192字节，不弱化边界断言；按实测修正assessment。

- D-006：本轮5555模拟器原生截图为1256×2760，并非上轮5557的1084×2412。目标仍是API24手机默认缩放浅色；按实际窗口更新UI环境记录，保留所有可读性/闭环断言，不把不同尺寸假报为同一设备。脚本按窗口尺寸取比例坐标已在此尺寸运行。

- D-007：T004的临时目录构建与T003模拟器触摸流程不存在代码/数据依赖，允许基于已通过T002先执行；总体passed和交付结论仍等待T003，所有验收条件不变。
