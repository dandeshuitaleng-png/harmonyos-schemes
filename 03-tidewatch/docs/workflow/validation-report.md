> 当前v0.2.0范围与验收以[演进方案](../evolution/blueprint.md)及[交付报告](../evolution/release-review.md)为准。本文件保留早期路线/验收历史。

# 本轮实施与验证报告

日期：2026-09-21。独立项目：`/Users/Admin/Projects/tidewatch-development/03-tidewatch`。
分支：`codex/tidewatch-development`。基线提交：`d05b4aa0aa1beaa0c3910eadddc9b3a2b7b257cf`。

## 结果

开发方案中的本轮“离线文字观察簿”已实现并完成自动化及手机模拟器核心流程验证。P1–P4完整产品路线图尚未实现，不能将本轮结果称为已具备线上审核或可直接上架的成品。

新增类型、风险、标题、地点、描述和有效期表单；支持编辑、检索筛选、过期历史、单条删除和全部清除确认。旧格式兼容读取，旧记录的分类标记为“未分类”，不推断未知观察类型。读失败禁止覆盖，写入互斥，保存失败后须重新读取。隐私说明与实际字段同步更新。

设备验证发现页面切换沿用滚动位置，已修复新建、编辑、保存、取消、隐私页切换的滚动复位。修复了 ArkTS 异常重抛类型与 Column 属性编译错误，并移除了页面 getContext 弃用调用。使用当前IDE的编译SDK，保留最低兼容API 23及目标API 23。

## 构建和自动化

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 领域及仓储测试 | 17/17通过，0失败 | [测试输出](../verification/automated-tests.txt) |
| TypeScript严格类型检查 | 通过，随test.sh执行 | 测试脚本先编译后测试 |
| HarmonyOS完整构建 | BUILD SUCCESSFUL，33任务，26s464ms | [构建输出](../verification/build.txt) |
| HAP压缩数据完整性 | unzip -t通过 | 本地运行记录 |
| 差异空白检查 | git diff --check通过 | 本地运行记录 |

自动化覆盖：必填和长度边界、枚举、有效期、重复时间生成不同ID、编辑不延长有效期、到期边界、JSON/分隔行迁移、损坏/空/未知版本/重复ID拒绝、过期历史保留、查询不修改全集、加载失败禁止写入、写失败后重新读取、并发互斥、文件适配器重建后恢复。

最终HAP：`entry/build/default/outputs/default/entry-default-unsigned.hap`，156,413字节。
SHA-256：`f5c4b5829cc648651f62d03e65701bb3b27fcdcc36c5b25153f0a6c4ec58565d`。

构建仍提示未配置签名、模块SemVer及部分SDK调用异常处理警告；不影响本次编译和模拟器安装。存储异常由仓储/页面处理并停止写入；尚未进行设备级磁盘故障注入。

## 专用手机模拟器验证

创建独立模拟器 `tidewatch_api24`，HarmonyOS 6.1.1 / API 24，本轮连接为`127.0.0.1:5559`，分辨率1256×2760。不要把这个端口假定为其他机器上的固定端口。已有API21模拟器低于项目最低兼容版本，安装失败符合兼容声明；已有API24模拟器初次安装成功后发现同时用于其他项目，后续所有测试转移到专用模拟器。

| 流程 | 实际结果 |
| --- | --- |
| 隐私同意→新建文字观察 | 标题、地点、描述成功保存，显示1/200及未上传 |
| 强制停止→重新启动 | 观察字段保留 |
| 修改标题和类型→保存→重新启动 | 更新保留，采集01:33、截止03:33不变 |
| 点击删除→取消 | 原观察仍在，[界面文本](../verification/restart-and-cancel.txt) |
| 撤回同意→重新同意 | 拒绝时隐藏记录，再次同意恢复1条，[界面文本](../verification/privacy-restored.txt) |
| 已过期筛选→全部 | 当前有效记录不在过期列表，切回全部恢复，[空态证据](../verification/expired-filter.txt) |
| 空表单保存 | 显示“标题需为2–40字”，不生成占位记录，[证据](../verification/invalid-form.txt) |
| 单条确认删除→强制停止→重启 | 0/200，记录未恢复，[证据](../verification/deleted-after-restart.txt) |
| 系统浅色/深色切换 | 实际截图检查通过，文字、卡片、按钮可读，长标题换行 |
| 切换到编辑页面 | 滚动复位到页头，修复后已重新安装验证 |

截图使用明确标记为“模拟”的测试内容，不是现实现场信息。测试结束已删除专用模拟器中的测试观察。

- [浅色截图](../verification/observation-light.png)
- [深色截图](../verification/observation-dark.png)

## 未完成的完整产品能力与验收

- 照片、定位、离线地图、关系型数据库、上传签名、服务端鉴权与审核、居民端及权威公告数据源：未实现，见开发方案P1–P3。
- 物理手机/平板、屏幕阅读器与大字体、整机重启、系统杀进程极限时序、实际断网切换和存储不足：尚未完成设备验收。已验证的是应用强制停止后的恢复；当前manifest不声明网络权限。
- 全部清除按钮的二次确认已实现，仓储空集合持久化已测试；未单独执行模拟器多记录全部清空用例。
- 未签名包在专用模拟器可安装；真机和上架需本应用对应的签名配置。本轮未读取、复制或提交私钥及签名密码。
- Preferences为小型文字簿的实现，200条上限。照片证据与大数据量需按P1迁移到数据库。
