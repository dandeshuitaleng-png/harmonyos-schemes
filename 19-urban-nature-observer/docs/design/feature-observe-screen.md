# 城市自然观察 · 观察与本机回顾

Route: `pages/Index` · 更新：2026-09-21。

沿用现有绿色语义色与暗色资源。顺序：观察边界 → 任务与提示 → 粗略环境 → 可见特征 → 主动保存 → 本机历史。正文16fp以上，按钮最小48vp。

选中状态由页面响应式状态直接读取，标签和背景同步更新；互斥选项即时替换。保存后清空本次特征，历史保留独立快照；再次观察回到顶部并沿用任务/环境。保存/删除处理中禁用相关控件；删除二次确认，返回键取消确认。

本机容量、草稿丢弃、删除和清除应用数据的后果均在界面明确说明。失败不显示成功，不覆盖损坏数据。

本轮手机模拟器浅色截图位于 `../development/evidence/`；真实设备、平板、深色、大字号及读屏体验仍待验证。
