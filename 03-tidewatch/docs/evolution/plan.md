# Plan L1 · Blueprint B1 → v0.2.0

所有步骤实施前固定量表：功能30、一致性20、维护15、验证15、UI10、数据边界10。总分≥90且每维≥80%，任何必需AC未通过仍阻断。自评不是独立审计。

检查项在此冻结：功能=本步必需AC逐项；一致性=映射/无越界；维护=类型清晰、模块依赖、错误传播、资源关闭、减少重复转换（最后一项非阻断，现有SnapshotIO过渡允许部分满足）；验证=相关回归、当前哈希、真实平台证据（T001/T004按对应基线/汇总证据）；UI=UI-001至004适用项；数据=不丢失/不复活/最小权限。项评分0/0.5/1。T001/T002没有改变生产视觉，UI N/A；T003/T004涉及UI使用或交付需UI验证，不得用未执行当N/A。

## T001 基线和方案（passed，第1轮）

依据B1、P-001至004、F-004、A-004、AC-001。依赖无。交付assessment/blueprint/ui-spec/plan/state/decisions及当前基线。
实现：复用已打开项目，保留工作树，检查CI和本机SDK；固定版本与上述验收。
检查：scripts/test.sh、scripts/build.sh、IDE窗口和代码清单；证据evidence/baseline-*。UI N/A（仅方案/基线，无UI变更）。回退：文档可逆修改，不改旧代码。
审查reviews/T001.md。通过后方可进入T002。

## T002 事务持久化与迁移（passed，第2轮）

依据B1、P-001/002/004、F-001/002/003、A-001/002/003/004、AC-002至005。依赖T001 passed。
范围ObservationSchema.ts、RdbSnapshotIO.ets、设备集成测试及SQL测试；暂不切换生产Store，方便隔离验收。
实现：按B1元数据标记/固定列/绑定参数/完整校验/原生事务/提交读回后清理偏好。故障触发器验证真实回滚。
交付新adapter及可复现临时测试HAP脚本；测试应用独立bundle，不放入生产路由。
检查：类型/构建、SQLite契约测试、专用模拟器原生测试结果；evidence/T002-*。UI N/A（新后端及测试页面仅输出机器报告，不是产品UI）。
风险：迁移和删除；回退：生产装配尚未切换，旧Store不变；测试只操作测试bundle沙箱。审查reviews/T002.md。

## T003 生产接入及用户闭环（passed，第1轮）

依据B1、P-001至004、F-001至003、A-001至003、AC-002/003/006、UI-001至004。依赖T002 passed。
范围ObservationStore装配、新隐私文案/加载提示、版本0.2.0；不重写页面和不增加权限。
交付可安装生产HAP，旧版实际升级与新建/编辑/删除/隐私/重启闭环。
检查：先旧HAP创建模拟记录，再覆盖安装v0.2；人工/脚本操作后截图与数据检查；evidence/T003-*。全维度适用。
回退：升级前保留旧HAP和测试沙箱证据；真实用户数据升级后不直接降级，不静默删除数据库。审查reviews/T003.md。

## T004 整体验收交付（passed，第2轮）

依据B1、P-001至004、F-004、A-004、AC-007。依赖T003 passed。
交付README、状态/覆盖矩阵/release-review与源码哈希；修正旧路线当前状态并引用唯一当前方案。
检查：受影响回归/守卫/干净源码复制构建；验证生产包没有测试路由。UI引用同一源码指纹的T003证据；新改动使其失效则重验。
回退：保留旧文档历史、证据不覆盖，代码不强推远程。审查reviews/T004.md。
