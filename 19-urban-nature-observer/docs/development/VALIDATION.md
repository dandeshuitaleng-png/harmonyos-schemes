# 本轮验证报告

日期：2026-09-21。分支：`dev/nature-19-improvements`。范围：仅19号工程。

## 已完成

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 独立克隆/工作区 | PASS | 独立目录、19号 `.code-workspace`、开发分支 |
| 单元/存储故障测试 | PASS，12/12 | `evidence/unit-tests.txt`；直接转译生产模型和存储类，Preferences 使用故障桩 |
| ArkTS / HAP | PASS | `evidence/build.txt`，BUILD SUCCESSFUL in 4 s 718 ms |
| 产物完整性 | PASS | `evidence/hap-integrity.txt`，ZIP 校验无错误 |
| 模拟器安装运行 | PASS | `127.0.0.1:5557`，API 24，1084×2412 |
| 选项互斥与状态刷新 | PASS | `smoke-initial.txt` 中选中/取消标签断言 |
| 保存后进程重启恢复 | PASS | `smoke-resumed.txt`，`03-restored.png` |
| 再次观察与原记录保留 | PASS | 同任务/环境恢复、特征未选中、新保存后2条 |
| 取消/确认删除及重启 | PASS | 取消后2条、删除后1条、重启仍1条 |
| 全部清空并重启 | PASS | 确认清空后0条、重启仍0条；`05-empty-after-restart.png` |
| 权限边界 | PASS（静态） | module 未声明网络、相机、定位权限；代码无上传逻辑 |
| 代码变更空白校验 | PASS | `git diff --check` |

最终HAP：`entry/build/default/outputs/default/entry-default-unsigned.hap`，95613 bytes。

SHA-256：`c109f08207bba11313ec098a416faf4be06d09fd23dd02fa1eb2166527a93211`。

## 测试中修复的问题

- 首次构建遇到仓库编译SDK 24与本机IDE 26不匹配：移除显式compileSdkVersion，使用IDE内置版本，保留API 23兼容下限。
- 模拟器发现Builder按值接收的选中布尔值不刷新：改为在Builder中直接读取页面响应式状态，选中标签与颜色随选择更新。
- 存储同时保护模型与界面：无效/未来版本数据拒绝覆盖；flush失败尝试恢复旧缓存并落盘；回滚失败暂停读写，要求重启核对；并发写入被拒绝。

初次模拟器脚本在保存后遇到前台切换，按设计停止，未继续向其他应用注入点击。因此完整流程由初次互斥检查加续跑重启/回访/删除组成；保留原始日志，不将中断伪装成一次全程通过。仅删除本轮测试创建的记录，最终记录数0。

## 构建警告与限制

- 无signingConfig；API 24模拟器接受未签名包，不能据此认定真机可安装或可上架。
- Hvigor仍报告entry版本SemVer警告（已有entry配置为0.1.0）。不影响本轮编译与模拟器安装，正式发布前需结合目标DevEco/ohpm版本复核。
- API 21模拟器安装被SDK兼容性检查拒绝，未降低产品最低版本绕过。
- 持久化故障是自动化桩测试；未在设备上做磁盘满、进程在flush中途被杀等故障注入。极端磁盘故障的最终状态需重启读取确认。

## 待完成的发布验收

1. 使用本项目的Debug签名和真实设备复测记录闭环，不复用其他项目签名。
2. 手机/平板、深色、放大字体、读屏焦点和返回键完整验收；目前只完成手机模拟器浅色截图复核。
3. 在设备上验证10条满额、快速连点、存储空间不足与升级保留；容量及并发规则已有自动化覆盖。
4. 按PLAN安排5名新手、3种植物情境的可用性访谈，测量5分钟完成率与鉴定误解率；没有实际教育效果数据。
5. 商店主体、隐私页面、截图、Release签名与商店审核。

结论：本轮离线MVP实现、自动化与模拟器核心闭环已完成。发布仍受上述门槛限制。
